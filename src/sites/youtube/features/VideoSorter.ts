// VideoSorter is currently disabled because it breaks YouTube's CSS Grid layout.
// The sorting feature will be re-implemented in a future version.

export class VideoSorter {
  constructor() {
    // Disabled
  }

  initialize(): void {
    // Disabled - does nothing
  }

  cleanup(): void {
    // Disabled - does nothing
  }

  reset(): void {
    // Disabled - does nothing
  }

  setEnabled( _enabled: boolean ): void {
    // Sorting is disabled until we find a safe way to reorder
  }
}
